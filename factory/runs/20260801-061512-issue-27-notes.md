# In progress: #27 dreamconnect_daemon: stale /dev/shm frame owned by a different uid blocks capture
branch: runtime/shm-frame-uid   worktree: /home/kogies/.factory/worktrees/dreamconnect/shm-frame-uid   started: 2026-08-01T02:10:52Z
seams: FrameBuffer.ensure() (runtime/dreamconnect_daemon.py:81) — reclaim shm file on PermissionError; ControlServer.run() (runtime/dreamconnect_daemon.py:498-507) — same treatment for control socket, parity. Both simulated via chmod 0o000 (not real cross-uid ownership). Gate: ./run-tests.sh

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 1 | FrameBuffer.ensure() unlinks+retries an inaccessible existing shm file | runtime/test_daemon.py::TestFrameBufferEnsure::test_ensure_recovers_from_foreign_owned_existing_file | module's wire-format contract (dreamconnect_daemon.py:39-48) + 0600-mode contract (ensure() comment :91-96), independently re-consumed by test_client.py's read_frame() | done | seraph pass |
| 2 | ControlServer.run() unlinks+retries an inaccessible existing socket path | dropped | dropped | dropped | oracle found ControlServer.run() already unconditionally unlinks before bind (dreamconnect_daemon.py:~505) regardless of file mode/owner — the EACCES issue #27 describes for the shm frame cannot occur here; unlink() is gated by parent-dir perms, not file owner. Verified with real ControlServer + mutant discrimination proof. Owner confirmed: drop slice, ship slice 1 only. No code change needed for socket parity. |
| 1 (breaker lap 1/2) | FrameBuffer.ensure() must also survive PermissionError from os.unlink() itself, not just os.open() | runtime/test_daemon.py::TestFrameBufferEnsureStickyBitUnlink::test_ensure_recovers_from_foreign_owned_file_under_sticky_bit_dir | module's 0600-on-create contract (dreamconnect_daemon.py:91-96) + header layout (:39-48) + Unix creator-ownership guarantee | superseded | builder's fix (daemon-side sudo fallback) rejected by owner: security-sensitive daemon->sudo path, and wouldn't work in prod (no sudoers grant exists). Owner decision: cleanup moves to install.sh (already root during migration). Daemon reverted to slice-1-only behaviour (unlink+retry for same-uid/stale-perm cases; genuine sticky-bit EPERM propagates by design). |
| 1b (guard) | FrameBuffer.ensure() raises cleanly (doesn't hang/swallow) when a file is structurally unreclaimable under sticky bit | runtime/test_daemon.py::TestFrameBufferEnsureStickyBitUnlink::test_ensure_raises_permission_error_for_a_foreign_owned_file_under_sticky_bit_dir | re-scope decision itself (daemon intentionally does not handle this case) | guard-test, passes, falsified via temporary swallow-and-return mutation | documents the daemon's contract post-rescope |
| 3 | install.sh reclaims a stale foreign-owned /dev/shm/dreamconnect.frame as root before daemon starts (new function reclaim_stale_shm_frame in install-lib.sh) | test_install.sh::test_reclaim_stale_shm_frame_unlinks_a_root_owned_file_under_a_sticky_bit_dir | task's own instruction + repro technique established by TestFrameBufferEnsureStickyBitUnlink (root-owned file under root-owned 1777 dir matching real /dev/shm stat) | superseded (breaker lap 2/2) | breaker: reclaim_stale_shm_frame unconditionally unlinks on EVERY install.sh run, incl. routine re-runs while the daemon is already live+same-uid — daemon never notices (ensure() only reopens on geometry change), systemctl enable --now is a no-op on an already-active unit (doesn't restart), and the freshly-restarted SC JVM (install.sh:349 always restarts SC) then hits FileNotFoundException on the now-missing inode and silently shows black forever. In-scope, introduced by this diff. Fix (same-class, in-scope, no new design — issue's own title says "owned by a DIFFERENT uid"): gate reclaim on ownership — only unlink if the existing file's owner uid != USER_UID (the daemon's target identity, resolved at install.sh:230). Breaker budget now exhausted (2/2 laps) — no further breaker round after this fix, final seraph + reviewer only. |
| 3b | reclaim_stale_shm_frame only reclaims when the existing file is NOT owned by the daemon's target uid (USER_UID) — leaves a live same-uid file untouched | test_install.sh::test_reclaim_stale_shm_frame_leaves_a_file_already_owned_by_the_target_uid_untouched (new) + test_reclaim_stale_shm_frame_unlinks_a_root_owned_file_under_a_sticky_bit_dir (updated for new target_uid param, retained intent) | issue #27's own title/body ("owned by a different uid") + install.sh's set -euo pipefail exit-0 requirement on the no-op path | done | reclaim_stale_shm_frame(path, target_uid) now compares stat -c %u to target_uid, only unlinks on mismatch. install.sh:337 passes $USER_UID. Both tests + full gate green. Final seraph: pass. Breaker budget exhausted (2/2). |
| 4 (guard, reviewer follow-up) | reclaim_stale_shm_frame's install.sh call site runs before the SC restart and daemon enable — pins the ordering that closes the residual stale-mmap gap | test_install.sh::test_install_sh_reclaims_the_stale_shm_frame_before_sc_restart_and_daemon_enable | reviewer's named gap + install.sh's own call-site comment | guard-test, passes, falsified by temporarily reordering install.sh (restored, verified identical) | reviewer verdict: ship with minor named caveats — this closes the one caveat worth fixing now (same-class, no new design, mirrors existing wait_for_user_bus ordering-test precedent). Other reviewer note (narrow unlink-vs-unlink TOCTOU race, effectively impossible under single-daemon systemd model) accepted as-is per reviewer. |

## Status: ready for /submit
bounces: 0 | guard_tests: 2 (rows 1b, 4)
