# In progress: #19 BootTests.java asserts stale evdev keycodes after H1 keysym rework
branch: agent/fix-boottests-evdev-keycodes   worktree: /home/kogies/.factory/worktrees/dreamconnect/fix-boottests-evdev-keycodes   started: 2026-07-28T00:00:00Z
seams:
  1. AwtEvdev package-private statics, exercised from dreamconnect.boot.BootTests (same package, plain javac, no JUnit), run by ./run-tests.sh.
  2. DreamConnectRobotPeer.keyPress/keyRelease -> wire string given to DaemonClient.input() (KS <keysym> <state> vs K <evdev> <state>), via a fake DaemonClient. Requires dropping `final` from DaemonClient.

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 1 | Rewrite testAwtEvdev() to post-H1 contract (keycode()->-1 for char keys, keysym() for A-Z/0-9/11 punctuation incl. non-identity VK_QUOTE=39/VK_BACK_QUOTE=96, fallbackKeysym(), functional keys unchanged); fix docs/design.md:63 | BootTests.java::testAwtEvdev,testAwtEvdevKeysymLetters,testAwtEvdevKeysymDigits,testAwtEvdevKeysymPunctuation,testAwtEvdevFallbackKeysym | AwtEvdev.java javadoc contract + X11 keysymdef/input-event-codes.h (external, not the impl) | done | Special case: production code (AwtEvdev) was already correct post-H1; only the test was stale. Oracle observed real red pre-edit (`FAIL: VK_A -> evdev 30`), corrected test, now green. No builder needed. Seraph: pass (Spec+Standards), recomputed all ~55 assertions independently, confirmed non-vacuous via mutation testing. |
| 2 | Routing test at DreamConnectRobotPeer proving KS-before-K precedence via fake DaemonClient (requires dropping `final` from DaemonClient) | BootTests.java::testRobotPeerKeyWire (+ FakeDaemon subclass) | runtime/README.md control-socket wire table; input-event-codes.h (KEY_ENTER 28, KEY_F1 59); X11 keysymdef (ASCII 'a'=97) | done (2 seraph laps) | Oracle agent was KILLED MID-RUN by weekly usage limit, but its work landed complete: DaemonClient `final` dropped (one-word prod change + comment, owner-approved at seam confirmation), FakeDaemon + testRobotPeerKeyWire added and wired into main(). Orchestrator ran `./run-tests.sh` directly: ALL PASS / ALL TESTS PASSED, EXIT=0. NOT yet independently gated by seraph — the oracle never reported its own derivation, so its reasoning is unverified beyond what is readable in the test source. VK_F1 trap assertion (expects `K 59 1`, not fallback keysym 112='p') is real and kills the fallback-first mutant. SERAPH LAP 1 = FAIL (Spec): test claimed to prove KS-before-K precedence but does NOT — AwtEvdev's KEY and KSYM tables are disjoint (0 vks in both across -1..0x20000), so KS/K ordering is behaviourally unobservable and the swap mutant survived. What it actually proves is table-before-fallback. Standards passed: 4/5 mutants killed, wire values recomputed correct against both emitter and daemon parser, FakeDaemon sound (ctor stores path only, `ensure()` lazy, `input()` override binds, null FrameReader never touched), `final` drop minimal and never ships (build.sh compiles only boot/). Fix applied: test's claims corrected to table-before-fallback with an explicit disclaimer that KS/K ordering is NOT proven; added `testAwtEvdevTablesDisjoint` guard (scans vk -0x100..0x20000, asserts no vk returns non--1 from both keycode() and keysym()). SERAPH LAP 2 = PASS (Spec+Standards): overclaim resolved, guard verified as SOLE detector of a `k(VK_INSERT,'i')` overlap mutant, range rationale confirmed exact by reflection (VK_CUT=0xffd1 is the highest AWT VK), README rewording accurate against source, slice 1 intact, 6 mutants tried and the only survivor is the documented-unobservable KS/K swap. Residual gaps seraph named: disjointness holds only over the scanned range (reflection over the private KEY/KSYM maps would make it total); end-to-end wire behaviour vs a live Mutter daemon is fake-only. Nit: BootTests.java:189 says "pins three things and no more" but lists four plus a count assertion — undercount, not an overclaim. |

## Out-of-scope findings and their classification (per /implement 4a)

1. **runtime/README.md:59 — `KS` described as "key by keysym (fallback)"** — surfaced by the oracle during slice 2.
   **Classification: same class as the work in hand → FIXED NOW, separate commit.**
   Reasoning: post-H1 keysym is the PRIMARY route for character-producing keys, not a fallback. This is
   the identical stale-doc defect that slice 1 already fixed at `docs/design.md:63`, in the exact area
   this change tests, and it directly contradicts the routing `testRobotPeerKeyWire` now pins. Needs no
   design decision; one line of prose. Fixed by the orchestrator; verified by seraph lap 2.
   Both `K` and `KS` rows reworded to name which key categories take which route.

2. **runtime/README.md — reply column says `OK` for `K`/`KS`, but the daemon returns `None`** —
   surfaced by seraph lap 1 as pre-existing drift.
   **Classification: anything else → FILE AN ISSUE, do not touch.**
   Reasoning: different concern (reply/ack semantics of the control socket, affects every command row,
   not just the key rows), pre-dates H1, and settling it needs a decision about whether the daemon or
   the doc is wrong. Out of scope for #19. TO FILE at /submit.

3. **runtime/test_client.py:71 — `"K 30 1"` labelled "K a press"** — noted by the architect.
   **Classification: same class, but NOT in the gate** — manual smoke script, not run by `./run-tests.sh`.
   Left alone deliberately; folded into the issue filed for (2) as a second stale-label instance.

## Breaker lap 1 — VERDICT: defect-found

**D1 (real, production). `AwtEvdev.java:101-105` + `DreamConnectRobotPeer.java:59-60`: `VK_SEPARATOR` (vk 0x6C)
falls through to `fallbackKeysym` and emits `KS 108` = the letter `l`.** Correct is `K 121` (KEY_KPCOMMA,
verified against /usr/include/linux/input-event-codes.h). Demonstrated live through the production peer +
fake daemon: `VK_SEPARATOR wire: [KS 108 1, KS 108 0]`.
Breaker enumerated all 188 AWT `VK_` constants by reflection: **VK_SEPARATOR is the ONLY vk in the entire
AWT space that reaches `fallbackKeysym`** — so the fallback branch's whole reachable input set is one key,
and it is wrong. The other 86 unmapped vks (F13-F24, COPY/PASTE/CUT/UNDO, all DEAD_*, AT/COLON/PLUS...) are
silently dropped.
Introduced by H1 (370451a): pre-H1 there was no fallback and VK_SEPARATOR was silently dropped, so H1 turned
a silent drop into a WRONG KEYSTROKE. Not introduced by this diff — but this diff is the one adding the test
that claims to cover `fallbackKeysym`, and that test picks '+' (0x2B) and '#' (0x23) as its "printable vk in
neither table" examples, **neither of which is an AWT vk value** (VK_PLUS=0x209, VK_NUMBER_SIGN=0x208) — so
both are unreachable through the peer while the one reachable case goes untested.
Missing test: `keyPress(VK_SEPARATOR)` -> `"K 121 1"`.

**D2 (in-scope, doc accuracy). `docs/design.md:63` is now wrong in a NEW way** — the line this diff wrote
asserts a two-way split ("everything else -> NotifyKeyboardKeycode"), but there is a THIRD route (best-effort
fallback keysym) and a FOURTH outcome (silent drop, 87/188 vks). `runtime/README.md:59` compounds it: dropping
"(fallback)" from the KS row is wrong because KS is still the carrier for fallback lines.

### Breaker REFUTED (covered ground — record in HAZARDS at /submit)
- `final` drop on DaemonClient is inert: build.sh:29-30 ships only agent/boot, test never ships; sole
  instantiation Bridge.java:79; class package-private in a jar appended via appendToBootstrapClassLoaderSearch
  from a root-owned temp file — a hostile subclass already requires owning the JVM. No exposure.
- FakeDaemon is not a latent hang: super() only stores the path; even if the ctor became eager, connect() to a
  nonexistent AF_UNIX path fails fast with ENOENT. No leak, no cross-test static state.
- All 54 evdev values in KEY verified against the kernel header — every one correct (incl. PRINTSCREEN->99
  KEY_SYSRQ, CONTEXT_MENU->127 KEY_COMPOSE, ALT_GRAPH->100 KEY_RIGHTALT; 125 shared by META/WINDOWS is
  intentional). All 47 keysyms are ASCII == X11 Latin-1. **No test/code-agree-but-both-wrong case found.**
- Disjointness is TOTAL today, not merely in-range: reflection over the private KEY/KSYM maps shows an empty
  intersection AND no key of either map outside -0x100..0x20000. The guard's range is not currently a blind spot.

### Breaker-noted test weaknesses (not defects)
- Drop path never asserted — no test presses e.g. VK_F13 and asserts zero wire lines.
- Disjointness guard escape: a vk outside the scanned range, e.g. extended keycode 0x0100007E from
  getExtendedKeyCodeForChar('~'), which Robot.keyPress accepts unvalidated. Theoretical; reflection over the
  maps would make the guard total.
- FakeDaemon.input drops the `synchronized` modifier its super declares. Inert single-threaded; poor pattern.

### Could not probe
Whether ScreenConnect's host actually emits VK_SEPARATOR (needs the SC client on an ABNT2/German numpad).
Bounds severity, not correctness. End-to-end vs a live Mutter session is fake-only here.

### D1 disposition (per /implement 4a) — OWNER DECISION
Owner asked at breaker lap 1 and chose **"Fix it now, separate commit"**.
Classification: **same class as the work in hand** — another instance of the H1 keysym-rework fallout this
change already touches, small, needs no new design. Context and tests are already built.
Slice 3 opened below. The broader question (whether `fallbackKeysym` should exist at all once VK_SEPARATOR is
mapped, since that makes the branch provably unreachable for every AWT vk) is a DESIGN decision, explicitly NOT
taken here — TO FILE as an issue at /submit, along with the untested silent-drop path for the other 86 vks.
D2 (doc accuracy) is in-scope repair of lines this diff itself wrote — folded into slice 3, not a separate commit.

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 3 | Fix VK_SEPARATOR wrong-keystroke (map to evdev 121 KEY_KPCOMMA) + correct docs/design.md:63 and runtime/README.md:59 to describe all FOUR outcomes (keysym / evdev / fallback / silent drop) | BootTests.java::testRobotPeerSeparatorKey | KeyEvent.getKeyText(VK_SEPARATOR)="NumPad ," vs VK_DECIMAL="NumPad ." (ran it); input-event-codes.h:198 KEY_KPCOMMA=121; xkb keycodes/evdev:204 + symbols/hu:759 binds <KPPT> to KP_Separator | done | Genuinely red, unlike slices 1-2. Exact red: `FAIL: keyPress(VK_SEPARATOR) -> "K 121 1" (KEY_KPCOMMA), not the 'l' keysym "KS 108 1" (got "KS 108 1")` + same for release. AMBIGUITY RESOLVED, and it corrected the orchestrator's brief: I had said KEY_KPDOT=52, but 52 is KEY_DOT (main-row period) and KEY_KPDOT is 83 — which AwtEvdev.java:80 ALREADY assigns to VK_DECIMAL. AWT keeps the two keys distinct, so 83 is ruled out and 121 stands unambiguously. Oracle also relabelled testAwtEvdevFallbackKeysym honestly as raw-function-only (kept all coverage) and machine-checked the "VK_PLUS=0x209 / VK_NUMBER_SIGN=0x208 are not AWT vks" claim so it cannot rot. OWNER CAVEAT (non-blocking): symbols/pc and symbols/us bind nothing to evdev 121 — only hu and keypad(...) variants do. So the fix turns "types l" into "correct separator where the layout has the key, nothing where it doesn't" — a strict improvement, not universal. The layout-independent alternative (KS 0xFFAC XK_KP_Separator) would contradict the repo's own numpad->evdev rule; oracle flagged rather than chose. |

### Slice 3 builder result
`AwtEvdev.java` one line in the numpad block: `m(KeyEvent.VK_SEPARATOR, 121); // KEY_KPCOMMA`.
No change to fallbackKeysym, KSYM, or DreamConnectRobotPeer. Builder edited NO test file (confirmed).
Both docs corrected to describe all four outcomes (keysym / evdev / printable-ASCII fallback / silent drop).
Gate: `./run-tests.sh` exit 0, 83 `ok :` assertions, 0 failures. Disjointness guard specifically re-verified —
VK_SEPARATOR (0x6C) is only in KEY, never in KSYM, guard still reports 0 overlaps.
Builder explicitly left uninvestigated: other unmapped-vk gaps (VK_HELP, VK_KP_* arrows) — fold into the
filed issue about the silent-drop path.

### Slice 3 SERAPH = PASS (Spec + Standards)
Independently confirmed: KEY_KPCOMMA=121 (input-event-codes.h:198); VK_DECIMAL still holds 83 (KEY_KPDOT) so
the two numpad keys stay distinct; evdev 121 has exactly one owner (vk 108); KEY size 55 / KSYM size 47 with a
totally empty intersection; the only duplicate evdev value is 125 (META/WINDOWS, pre-existing and intentional);
nothing outside AwtEvdev/BootTests enumerates the KEY table (the daemon parses `K <int>` generically).
Non-vacuity proven twice: reverting the one line reproduces the oracle's red BYTE-IDENTICALLY, and a 121->83
mutant also fails. Builder touched no assertion — decisive evidence is that the expected value `K 121 1`
appears verbatim in the oracle's recorded red, i.e. it pre-dates the implementation.
Slices 1 and 2 confirmed not regressed.

### Seraph nits on slice 3 (recorded, not fixed — none are defects)
- Builder claimed BOTH docs describe four outcomes; `runtime/README.md` describes three (silent drop absent).
  Arguably correct for a wire table, since a drop emits no line. The doc is not wrong; the builder's CLAIM
  slightly overstated it. Left as is.
- Neither doc states that the printable-ASCII fallback is now unreachable for EVERY named AWT vk (reflection
  after the fix: `vks still reaching fallbackKeysym: []`; 86 vks silently dropped). Not a misrepresentation —
  a raw int from Robot.keyPress could still reach it — but it reads as a live route. Folded into the filed issue.
- PRE-EXISTING, untouched: `runtime/README.md:62` points at `../keymap/`, which does not exist.
  **Classification: anything else -> FILE AN ISSUE.** Fixing it needs someone to determine what it should point
  to (or whether the sentence should go), which is a decision, not a typo. TO FILE at /submit.

### Breaker lap 2: SKIPPED — and why
Breaker budget was 2 laps; only lap 1 was used. Lap 2 skipped deliberately, not overlooked. Since lap 1 the diff
gained exactly one production line plus doc prose, and the seraph already subjected that line to the checks a
breaker lap would run: revert-mutant and wrong-value-mutant both fail the suite, evdev-value collision checked by
reflection over the private tables, disjointness guard re-verified, and a repo-wide grep confirmed nothing else
depends on the KEY table's contents. The residual risk the breaker named is layout semantics (does `K 121`
actually produce a separator on a given guest keymap), which no static red-team pass can settle — it needs a live
SC session on an ABNT2/hu numpad. Recorded so nothing is credited as ruled out that was never probed.

## Reviewer verdict: SHIP — no blocking finding
Independently reconfirmed by reflection: 0 named AWT vks reach fallbackKeysym after the fix (86 dropped);
VK_CUT=0xffd1 is the max vk; KEY_KPCOMMA=121 / KEY_KPDOT=83 match the kernel header. Breaker's refutations hold.
Reviewer explicitly cleared: the `final` drop, the javadoc weight, and leaving fallbackKeysym live (the branch IS
genuinely reachable from a raw int — 13 values in 0x20..0x7E — so the docs are not lying).

### Final round — all four reviewer findings CLOSED
1. **Silent-drop path untested (the important one).** Added `BootTests.java::testRobotPeerUnmappedKeyDropped`.
   Falsified against a scratch mutant appending `else daemon.input("KS " + awtVk + " " + state)` to sendKey:
   `FAIL: ... keyPress(VK_F13) puts NO line on the wire (got [KS 61440 1])` + release + a nonsense-vk case,
   3 FAILURE(S). **Decisive: all 83 pre-existing assertions stayed `ok` under that mutant** — the gap was real,
   and `KS 61440 1` is the D1 wrong-keystroke defect class reproduced. Now closed.
2. **runtime/README.md reply column.** M/B/W/K/KS `OK` -> `—`. **My earlier 4a deferral of this was WRONG and the
   reviewer corrected it**: I had classified it "needs a decision about whether the daemon or the doc is wrong",
   but dreamconnect_daemon.py:530-532 states the intent outright ("Input commands are fire-and-forget: they return
   None") and every one of those five handlers does `return None` — I verified this in the source myself. No design
   decision was needed; it was a plain doc error. Fixed all five rows, not just the two this diff had touched, so
   the table is not left self-contradicting. PING/GEOM/NODE verified correct and unchanged. The lead-in "one reply
   line per command" was also wrong and is corrected.
3. **BootTests.java:189 self-contradicting javadoc** ("pins three things and no more", then listed five).
   Same overclaim class seraph lap 1 failed this slice for. Now "pins five things", enumerated.
4. **docs/design.md:63 510-char table cell** (neighbours ~100) — readability regression this diff introduced.
   Four outcomes moved to a `#### Key routing` block below the table; cell left terse.

### FINAL STATE — ready for /submit
`./run-tests.sh` exit 0: Java 86 assertions ALL PASS, Python 12 tests OK, ALL TESTS PASSED.
Production diff is 2 files / 4 insertions / 2 deletions total:
  - `AwtEvdev.java`: +1 line, `m(KeyEvent.VK_SEPARATOR, 121); // KEY_KPCOMMA`
  - `DaemonClient.java`: `final class` -> `class` + explanatory javadoc line
`DreamConnectRobotPeer.java` never touched. Everything else is tests and docs.

### CAVEAT ON THE FINAL ROUND — read before /submit
The four fixes above were **NOT independently re-gated by a fourth seraph run** (usage-limit budget; a weekly
limit was already hit once during this run and killed one agent mid-flight). They are additive test + doc changes
on top of a diff that passed three seraph gates. What backs them: the new drop-path test carries explicit
falsification evidence, and the orchestrator verified the README reply-column claim directly against
dreamconnect_daemon.py:525-550 and ran the full gate. The doc restructures (findings 3 and 4) rest on the oracle's
report alone. If /submit wants belt-and-braces, one more seraph pass over the final diff would close that.

### COMMIT SPLIT (reviewer's, adopted)
Commit 1 — issue #19: BootTests.java (all test work), docs/design.md, runtime/README.md, DaemonClient.java.
Commit 2 — unrequested same-class bug fix, owner-approved: AwtEvdev.java's one VK_SEPARATOR line +
  testRobotPeerSeparatorKey. Message must state why unrequested work is present.
Reviewer's reasoning: both doc edits describe the H1 split, not the separator, so putting them in commit 2 would
make commit 1 land a design.md line already known to be wrong.

### ISSUES TO FILE AT /submit
a. `fallbackKeysym` is unreachable for every NAMED AWT vk after the VK_SEPARATOR fix (reachable only from a raw
   int via Robot.keyPress). Delete the branch or keep it? DESIGN decision, deliberately not taken here.
   Fold in: the 86 silently-dropped vks (VK_F13-F24, COPY/PASTE/CUT/UNDO, all DEAD_*, AT/COLON/PLUS...) — is
   dropping right, or should they map? Builder also left VK_HELP and the VK_KP_* arrows uninvestigated.
b. `runtime/test_client.py:71` labels `"K 30 1"` as "K a press" — stale post-H1. Manual smoke script, not in the
   gate, so left alone deliberately.
c. `runtime/README.md:62` points at `../keymap/`, which does not exist. Pre-existing; fixing it needs someone to
   decide what it should point to or whether the sentence goes.
d. `testAwtEvdevTablesDisjoint` asserts an unbounded invariant with a bounded scan (-0x100..0x20000); an extended
   keycode >=0x01000000 would escape it. Reflection over the private KEY/KSYM maps would make it total and O(102).
   Reviewer graded this a preference, not a smell — the javadoc states the limit.
