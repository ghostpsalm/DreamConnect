# In progress: issue #43 — agent/build.sh: verify ByteBuddy jar's SHA-256 after download
branch: agent/build-integrity   worktree: /home/kogies/.factory/worktrees/dreamconnect/build-integrity   started: 2026-08-01T00:00:00Z
seams: agent/build.sh exit code + stderr, exercised against a sandboxed fixture jar (corrupted vs. correct), covering both the cache-hit and fresh-download paths

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 1 | pin BB_SHA256 constant + sha256sum -c check in build.sh, unconditional (both cache-hit and fresh-download paths); new agent/test_build.sh; wire into run-tests.sh; note in agent/README.md | agent/test_build.sh::test_a_poisoned_cached_jar_fails_the_build_before_anything_is_produced (+ ::test_a_correctly_hashed_cached_jar_still_builds, ::test_the_real_agent_lib_cache_is_never_written) | Maven Central .sha256 sidecar for byte-buddy-1.18.11.jar, cross-checked against locally cached jar's sha256sum: e32f454c2c1f4aca982f9ec764ed892d9a6eee7e8a77f435cbdd180f6ffdb821 | self-heal fix green, gate green, seraph: pass | breaker lap 2/2 (final, budget exhausted): found (1) TOCTOU — verify then reread, bytes hashed aren't the bytes javac/unzip later consume, adjacent+needs-design-decision, filing issue; (2) rm -f fires on ANY sha256sum nonzero exit (incl. tool-missing/127), not just genuine mismatch, misleading "removed the rejected jar" message and could delete a good jar — in-scope, small, fixing now (last round, no further breaker lap); (3) concurrent-build race on lib/, low/self-healing, bundled into filed issue #48 (TOCTOU), not fixing. Filed #48. Oracle wrote test_an_unrunnable_verifier_fails_closed_without_destroying_the_cached_jar, red confirmed (5 failures), builder fixed (preflight command -v sha256sum check), seraph final pass: pass. Reviewer: SHIP. Filed #49 (sha256sum -c path-parsing edge case, low severity, optional). Reviewer flagged: gate green does NOT include Case B (happy-path full build) in this worktree — it skips for lack of a local cached jar; must be stated honestly in the submit report, not glossed as "gate green" alone. **done** |
