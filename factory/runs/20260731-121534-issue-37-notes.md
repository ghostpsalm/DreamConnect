# In progress: issue #37 — Stale references in runtime/ docs and smoke script
branch: docs/runtime-refs   worktree: /home/kogies/.factory/worktrees/dreamconnect/runtime-refs   started: 2026-07-31T12:08:49Z
seams: doc/comment/string-literal only, no runtime code path — direct edit-and-verify, no oracle/builder red-green (confirmed with owner)

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 1 | runtime/test_client.py:65-72 — K 30 -> KS 97 for 'a' | none (no assertable behavior) | AwtEvdev.java keysym mapping | done | direct edit-and-verify; gate green |
| 2 | runtime/README.md:62 — repoint ../keymap/ to AwtEvdev.java | none (no assertable behavior) | agent/boot/dreamconnect/boot/AwtEvdev.java existing path | done | direct edit-and-verify; gate green |

## Out-of-band finding (resolved, no action needed here)
Builder found `./run-tests.sh` red on the pre-merge base: `BootTests.java:37` asserted the stale
`VK_A -> evdev 30` mapping. Turned out to be issue #19, already fixed on `origin/main` via `fd47417`
("Keyboard: realign BootTests with the H1 keysym split, fix numpad separator (#39)"), closed
2026-07-31T11:21:11Z — this branch (docs/runtime-refs) was simply cut before that commit and had no
commits of its own, so fast-forwarded via `git merge --ff-only origin/main`. That commit also touched
`runtime/README.md` (reply-column rewrite) but left the `../keymap/` line (issue #37 item 2) untouched,
so both of this run's edits were still needed and merged cleanly on top. Not scope creep — resolved by
picking up already-landed work, nothing new was fixed here.

Breaker and reviewer skipped: both edits are doc/comment/string-literal only, no logic, matching the
loop's explicit "genuinely mechanical" / "doc edit, version of a string" exception.
