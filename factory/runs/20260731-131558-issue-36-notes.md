# In progress: #36 — fate of fallbackKeysym and the 86 silently-dropped vks
branch: agent/keyboard-tables   worktree: /home/kogies/.factory/worktrees/dreamconnect/keyboard-tables   started: 2026-07-31T12:08:33Z

## Prerequisite (done before slice 1)
Fast-forwarded onto origin/main to pick up fd47417 (#19/#39 fix + realigned
BootTests) — this worktree was one commit behind. Clean FF, no divergence.

## Agreed seams (owner-confirmed)
1. **fallbackKeysym: DELETE.** sendKey's third branch becomes an explicit,
   documented drop. Owner confirmed: ScreenConnect appears to only ever pass
   named VK_* constants; the raw-int safety net has no observed use case.
2. **Map only F13-F24, VK_HELP, VK_KP_UP/DOWN/LEFT/RIGHT (17 of 86 vks).**
   Values from /usr/include/linux/input-event-codes.h (KEY_F13=183..KEY_F24=194,
   KEY_HELP=138) and the existing VK_UP/DOWN/LEFT/RIGHT evdev codes
   (103/108/105/106) for the KP-arrow aliases. The other 69 stay dropped —
   documented as a deliberate contract (clipboard/edit commands, VK_DEAD_*,
   IME cluster, shifted-symbol vks, VK_UNDEFINED, VK_BEGIN — none have a
   meaningful evdev/X11 physical-key equivalent).
3. **VK_SEPARATOR layout independence: OUT OF SCOPE.** Filed as a separate
   follow-up: https://github.com/ghostpsalm/DreamConnect/issues/40

Test seam for all of the above: `DreamConnectRobotPeer.sendKey` via `keyPress`/
`keyRelease` against `FakeDaemon` (existing pattern in BootTests.java),
asserting the wire line. Table entries also asserted directly via
`AwtEvdev.keycode`/`keysym`. Expected values come from input-event-codes.h,
never from the implementation.

## Slices

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 1 | Delete fallbackKeysym; sendKey's third branch is an explicit drop | BootTests.java::testRobotPeerNoPrintableAsciiFallback, ::testRobotPeerUnmappedKeyDropped (repointed off VK_F13 onto VK_DEAD_GRAVE/VK_COPY/VK_UNDEFINED), ::testNoNamedAwtVkNeedsTheDeletedFallback (new reflective guard) | grep evidence: fallbackKeysym had exactly one caller (sendKey); JDK KeyEvent.VK_PLUS/VK_AT/VK_DEAD_TILDE reflection | done | seraph pass (Spec+Standards), breaker found 1 doc-drift defect (runtime/README.md:62, same-class fix applied same slice, no new commit yet) |
| 2 | Map VK_F13-VK_F24 to evdev KEY_F13..KEY_F24 | BootTests.java::testAwtEvdevF13ToF24Mapped (new) | /usr/include/linux/input-event-codes.h (KEY_F13=183..KEY_F24=194) | done | |
| 3 | Map VK_HELP to evdev KEY_HELP | BootTests.java::testAwtEvdevHelpMapped (new) | /usr/include/linux/input-event-codes.h (KEY_HELP=138) | done | |
| 4 | Map VK_KP_UP/DOWN/LEFT/RIGHT to the same evdev codes as VK_UP/DOWN/LEFT/RIGHT | BootTests.java::testAwtEvdevKpArrowsMapped (new) | existing AwtEvdev table entries for VK_UP/DOWN/LEFT/RIGHT (103/108/105/106) | done | |
| 5 | Document the drop contract (docs/design.md): explicit list of the 17 mapped vs. 69 deliberately excluded vks, and why | none (docs) | N/A | done | runtime/README.md's stale fallback line was already fixed in slice 1 after breaker found it; nothing else there needed touching. Seraph lap 2: found 2 doc-accuracy defects (incomplete edit/action cluster, false "no evdev code" claim for shifted symbols) — fixed, re-gated pass. Breaker (slices 2-5, cumulative): no-defect-found. |

## Bounce/guard-test tally
bounces=0, guard_tests=0 (all 5 slices proceeded oracle->builder->green without a bounce; no oracle-observed vacuous-green requiring a guard-test classification)

Note: testRobotPeerUnmappedKeyDropped currently uses VK_F13 as its "still
dropped" exemplar — slice 1's builder must repoint it at a vk that stays
dropped after slice 2 (e.g. VK_UNDEFINED or a VK_DEAD_* constant), or it goes
vacuous once F13 is mapped. Flag this dependency to the oracle/builder for
slice 1 and slice 2.
