# In progress: #38 — make testAwtEvdevTablesDisjoint total instead of a bounded scan
branch: agent/keyboard-tables   worktree: /home/kogies/.factory/worktrees/dreamconnect/keyboard-tables   started: 2026-07-31T13:30:00Z

## Agreed seam
Single seam: `BootTests.testAwtEvdevTablesDisjoint` itself, rewritten to reflect over
`AwtEvdev.KEY`/`KSYM` (private static final Maps) instead of scanning a bounded vk
range. No production code change — `AwtEvdev`'s API surface stays curated
(keycode/keysym/button only), matching the architect's read of the codebase's
convention against test-only accessors.

## Same-class fix bundled into this slice (not a new design decision)
`BootTests.java` lines 385, 489, 530, 577 cite `factory/CHECKPOINT.md, "Agreed
seams" ...` from #36's work. That checkpoint was archived per /submit step 7
(now `factory/runs/20260731-131558-issue-36-notes.md`) and the live file no
longer exists — these are dangling references in the same file this slice
already touches. Repointing them to the archived path as part of this slice's
commit; not filing separately since it's mechanical and same-file.

## Slices

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 1 | Total (reflection-based) disjointness check, replacing the bounded scan | BootTests.java::testAwtEvdevTablesDisjoint (rewritten) | Current AwtEvdev.KEY/KSYM contents via reflection (KEY=72, KSYM=47, intersection empty, independently verified) + a synthetic overlap mutant to prove the rewrite actually catches one | done | guard-test: passed against current code, falsified via scratch mutant (general overlap + the >=0x01000000 extended-keycode case the old bounded scan couldn't see). No builder needed, no production code changed. |
