# In progress: #18 display-host service account for unattended reboot survival
branch: install/display-host   worktree: /home/kogies/.factory/worktrees/dreamconnect/display-host   started: 2026-07-27T12:38:28Z

seams:
1. Sourceable installer: extract `install-lib.sh` (pure functions, incl. existing `gdm_conf`/`enable_autologin`/`disable_autologin`), sourced by `install.sh`.
2. `run()` indirection + `DC_DRY_RUN=1` around every `useradd`/`userdel`/`loginctl`/`systemctl`/`dconf` call — the only way slices are testable without root.
3. Fixture-path overrides: `DC_PASSWD_DB`, `DC_ACCOUNTSSERVICE_DIR`, `DC_DCONF_DIR`, `DC_STATE_FILE`, `DC_GDM_CONF` (default to real paths).
4. New `test_install.sh` (bare bash asserts, no framework — matches run-tests.sh style), wired as a third section of `run-tests.sh`. Must refuse to run as uid 0.
5. Socket-path formula `/run/user/<uid>/dreamconnect.sock` defined once in `resolve_host_identity`, consumed by the agent drop-in's `@UID@` substitution.

owner decisions (2026-07-27):
- Seam 1: install-lib.sh extraction (not main()+BASH_SOURCE guard).
- Dconf scope: expanded set B (issue's 3 keys + idle-activation-enabled=false, sleep-inactive-ac/battery-type='nothing', gnome-initial-setup-done=yes) — closes the same failure class the feature exists to prevent.
- Uninstall: add `/etc/dreamconnect/install.state` (HOST_ACCOUNT/HOST_UID/CREATED_ACCOUNT/AUTOLOGIN_SET) so uninstall knows what's safe to remove.
- Same-class fix approved (4a): `enable-linger` is never disabled on uninstall today (pre-existing gap, same uninstall-reversal code path this feature already touches). Fix now, separate commit.

high-risk rails (must hold for every account-deletion-capable slice): never touch uid 0; never delete uid>=1000 account unless CREATED_ACCOUNT=1 AND GECOS marker `DreamConnect display host` matches; never touch SUDO_USER or the detected desktop user; never a home of `/`, `/home`, `/root`. Never write `/etc/dconf/db/local.d` (would disable lock for every user on the box) — private profile only.

| # | slice | test (path::name) | expected value from | status | note |
|---|-------|-------------------|---------------------|--------|------|
| 1 | install-lib.sh extraction + run()/DC_DRY_RUN + test_install.sh harness wired into run-tests.sh (fixture-path overrides DC_PASSWD_DB etc. belong to slices 2-6, not this one — corrected from an earlier misstatement in this row) | test_install.sh::test_sourcing_is_side_effect_free, ::test_library_defines_the_installer_functions, ::test_run_executes_the_command_by_default, ::test_run_suppresses_execution_when_dry | issue #18 Gate + slice spec for run() | done | verifier confirmed green; shellcheck unavailable in this env (gate step skipped per its own "if available" wording) |
| 2 | resolve_host_identity (account→uid/home/socket, empty=fallback/back-compat) | test_install.sh::test_resolve_host_identity_defaults_to_fallback_user, ::test_resolve_host_identity_prefers_the_named_account, ::test_resolve_host_identity_socket_is_run_user_uid_dreamconnect_sock, ::test_resolve_host_identity_fails_when_the_account_is_absent | issue #18 seam 1/5 + "never invents a uid" | done | verifier confirmed green, mutation-tested (prefix-match/fabricated-uid/no-fallback/wrong-socket mutants all caught) |
| 3 | write_install_state/read_install_state + host_account_removable safety rails | test_install.sh::test_library_defines_the_state_and_removal_functions, ::test_write_install_state_creates_parent_dirs_and_all_four_keys, ::test_write_install_state_overwrites_rather_than_appends, ::test_install_state_round_trips_all_four_values, ::test_read_install_state_resets_to_safe_defaults_when_absent, ::test_host_account_removable_accepts_the_account_we_created + 9 refusal-rail tests (uid0, dangerous homes, protected_user, sudo_user, state-not-created, state-absent, state-names-another-account, wrong-gecos, unknown-account) | slice-3 six-rail contract + issue #18 uninstall reversibility | done | verifier confirmed green, independently mutation-tested rails 4+5+6; accepted gap: rail-1 (account absent) has no independent test since empty-GECOS makes rail-6 catch it too — verifier judged acceptable, not a blocker |
| 4 | ensure_host_account (create-if-absent, hidden, no-sudo, disabled password, idempotent, reports ACCOUNT_WAS_CREATED) | test_install.sh::test_library_defines_ensure_host_account, ::..._creates_a_system_account_with_the_marker, ::..._grants_no_supplementary_groups, ::..._disables_the_password, ::..._writes_nothing_when_dry, ::..._skips_useradd_when_the_account_exists, ::..._writes_the_hidden_marker_file_idempotently, ::..._passes_the_marker_as_one_argument, ::test_ensure_host_account_reports_whether_it_created_the_account | issue #18 agreed design + GECOS marker from rail 6 + coordinator decision on ACCOUNT_WAS_CREATED caller-scope convention (mirrors read_install_state) | done | verifier confirmed green, 7/7 independent mutations killed, GECOS marker byte-identical to host_account_removable confirmed |
| 5 | configure_no_idle_lock / remove_no_idle_lock (expanded set B: 7 dconf keys + environment.d DCONF_PROFILE + gnome-initial-setup-done marker + chown home files) | test_install.sh::test_library_defines_the_idle_lock_functions, ::..._writes_the_dconf_profile, ::..._writes_all_seven_dconf_keys, ::..._points_the_session_at_the_profile, ::..._skips_gnome_initial_setup, ::..._refuses_reserved_dconf_names, ::..._is_byte_identical_on_a_second_run, ::..._chowns_the_home_files_to_the_account, ::..._survives_a_failing_chown, ::test_remove_no_idle_lock_reverts_everything_configure_wrote, ::..._is_a_no_op_when_nothing_was_configured, ::test_configure_no_idle_lock_gates_only_dconf_update_behind_run | owner-approved expanded set B + install.sh:235 chown precedent | done | verifier confirmed green, 5/5 mutations killed. CARRIED TO SLICE 6: remove_no_idle_lock has no reserved-name guard (user/local) — currently unreachable since configure_no_idle_lock always refuses those names first, but slice 6's uninstall reads HOST_ACCOUNT from a state file on disk, so add the same guard directly to remove_no_idle_lock, not just the caller (verifier's recommendation) |
| 6a | uninstall_host_account (loginctl disable-linger/terminate-user suppressed, userdel -r propagates its own exit status, gated by host_account_removable) + remove_no_idle_lock reserved-name guard | test_install.sh: 6 original + test_uninstall_host_account_reports_a_failing_userdel + test_..._survives_set_e_when_loginctl_fails (52 tests total) | slice-6a contract, corrected after seraph found unconditional return-0 swallowing userdel failures | done | one full fail→fix→pass cycle: seraph caught the swallowed-failure defect, fixed; second seraph pass found the fix untested under install.sh's real set -e, closed with a guard test proving || true is load-bearing (mutation-confirmed) |
| 6b | install.sh wiring: resolve_host_identity + conditional ensure_host_account/configure_no_idle_lock/write_install_state on DREAMCONNECT_HOST_ACCOUNT opt-in; retarget autologin/@UID@ sed/linger/user-unit; rewrite uninstall() via read_install_state + remove_no_idle_lock + uninstall_host_account + fix pre-existing linger-not-disabled gap (owner decision #4) | none — install.sh top-level flow is not unit-testable without root (same limitation as slice 1); validated by bash -n + manual trace + live slices 5-8 on physical box | issue #18 seam 1/2 + owner decisions | done | Extended verification: 4 seraph passes total (bound reached once, owner consulted and approved fixes). Fixed+confirmed: write_install_state timing; uninstall cleanup no longer aborts on remove_no_idle_lock refusal; AccountsService marker reverted on uninstall; 3 bare `gdm_conf` set -e aborts (uninstall + 2x install flow); rail-3 collision where host_account_removable refused to ever delete the account on its own autologinned steady-state (fixed by nulling protect_arg when it equals HOST_ACCOUNT, locked in via oracle guard test); headless/SSH-only uninstall (PROTECTED_USER made best-effort, classic-mode die() restored); a regression that fix introduced in monitor-detection's RUN_USER construction. Final pass found 2 more (S1: SC_UNIT detection, S2: PM detection) — confirmed PRE-EXISTING (byte-identical in HEAD, not introduced by this slice), fail-safe (abort not corrupt), different area than this issue's seams — filed as https://github.com/ghostpsalm/DreamConnect/issues/20 rather than fixed here, per the same-class/different-class rule. Also noted, accepted as residual: a narrow failure window inside ensure_host_account (post-useradd, pre-state-write) — root-only, would need live-box testing to close further. Suite: 53/53 |
| 7 | fix: enable-linger never disabled on uninstall (pre-existing bug) | | | pending | same-class fix per 4a, separate commit |

| 7 | breaker fix-up: valid_account_name (rejects numeric/path-traversal/overlong names) + 7th rail on host_account_removable + guards on configure_no_idle_lock/remove_no_idle_lock/remove_accountsservice_marker/ensure_host_account + ensure_host_account backs up a pre-existing AccountsService file (.dreamconnect.bak) + remove_accountsservice_marker restores it on uninstall + install.sh entry-point validation of DREAMCONNECT_HOST_ACCOUNT | test_install.sh: 21 new (valid_account_name x8, per-function malformed-name refusal x5, ensure_host_account backup/restore x6, remove_accountsservice_marker round-trip x2) | breaker findings #1/#2/#3/#5 (data loss, uid-hijack, path-traversal guard bypass, permanently-wedged uninstall) | done | first breaker pass found 6 defects; #1/#2/#3/#5 fixed here (closed via one shared validator + backup/restore), #4 (dconf-update-fails-before-autologin) judged correct-as-is (issue's own constraints require failing loudly rather than shipping unconfirmed idle-lock config; state already written so retry is safe), #6 (linger/session race) needs root, noted below. Fix-up round itself needed 2 seraph loops: first found remove_accountsservice_marker had no guard of its own (exploitable path-traversal), second closed it + found ensure_host_account (the actual useradd caller) was the sole unguarded function of the six — fixed. Suite: 74/74 |

live slices (5-8 in issue #18, manual observation on the physical test box, not this session): create account+autologin+reboot; PING/PONG from root SC JVM; confirm no idle-lock; confirm --uninstall fully reverts. Also confirm defect #6 (whether `systemctl --user daemon-reload`/`enable --now` depend on linger having synchronously started `user@<uid>.service` for a never-logged-in host account) doesn't need a fix — reasoned as plausible but unconfirmed without root. Record results in ROADMAP.md, not here.

| 8 | breaker lap 2 fix-up: backup/restore for the two configure_no_idle_lock/remove_no_idle_lock home-directory files (environment.d/dconf-profile.conf, gnome-initial-setup-done) + the two dconf-system files (profile/<name>, db/<name>.d/00-display-host — keyfile backup deliberately placed OUTSIDE the .d directory to avoid dconf compiling the backup itself as live config) | test_install.sh: 12 new (6 home-file: backs-up-preexisting/writes-no-backup-fresh/backs-up-once/restores/removes-fresh/backs-up-independently; 6 dconf-system: same 6 shapes) — suite now 86 total | breaker lap 2 finding #1 (must-fix: DREAMCONNECT_HOST_ACCOUNT pointed at an existing account silently clobbers + deletes its dconf profile with no backup) + orchestrator's own follow-up extending the same fix to dconf-system files for internal consistency | done | completes 3 backup/restore pairs this session (AccountsService marker, home files, dconf-system files) — every destructive overwrite/delete in the feature that could clobber pre-existing third-party content now has one. Directory-removal ordering (restore-before-rmdir, mkdir -p guard) mutation-tested. Two pre-existing, low-severity, not-fixed items noted by final seraph: enable_autologin's .bak is never removed on uninstall (orphaned backup file, no data loss — disable_autologin strips keys surgically instead of restoring wholesale); the daemon service file has no backup (no third-party content risk — the filename is unique to this feature). Suite: 86/86 |

known follow-up issues filed, not fixed on this branch (pre-existing, different area than this issue's seams): #19 (stale BootTests.java evdev keycodes after H1 keysym rework — blocks ./run-tests.sh reaching the installer/python sections), #20 (SC_UNIT/PM detection can abort install.sh under set -e on a box with no matching unit/package manager — now higher-stakes since account cleanup sits behind it, but unchanged since before this feature), #21 (DREAMCONNECT_HOST_ACCOUNT pointed at an existing account: install.state is single-slot with no reconciliation across renamed/re-run identities; remove_no_idle_lock doesn't validate its home argument; valid_account_name accepts "root" and is locale-dependent).

breaker history: lap 1 found 6 defects (4 fixed: state-write timing, uninstall-abort-on-refusal, AccountsService marker never reverted, 3x bare gdm_conf set -e abort, rail-3 self-protection collision, headless/SSH uninstall; #4 dconf-update-fails-before-autologin judged correct-as-designed; #6 linger/session race needs root, noted for live testing). lap 2 (final, per 2-lap budget) found 4 more (#1 fixed as slice 8 above; #2/#3/#4 filed as #21). No 3rd breaker lap per the loop's own bound.

reviewer pass: approved scope/safety-margin/pattern-consistency; found 1 blocking defect + 2 worth fixing now + docs gaps. All fixed (slice 9): (1) uninstall() deleted the state file unconditionally even when uninstall_host_account failed (e.g. userdel exits 8, account busy) — permanently orphaning an undeletable account since state agreement is rails 5/6 of the removal gate; now gated on actual removal success, tracked via account_removed. (2) closed #20 in this branch rather than leaving filed — the reviewer argued this diff is specifically what makes that pre-existing set -e fragility newly block a critical NEW behavior (uninstall's account cleanup); both SC_UNIT and detect_pm's call site now `|| true`, issue closed. (3) README.md + ROADMAP.md H6 updated to document DREAMCONNECT_HOST_ACCOUNT, status marked pending live verification not DONE. Reviewer's other 2 items (enable_autologin's orphaned .bak on uninstall; #20's stricter framing) — the .bak item is pre-existing (not introduced by this feature) and low-severity (orphaned backup file, no data loss) — noted here, not filed as a 4th issue given #19/#20/#21 already cover the outstanding pre-existing gaps. Suite: 86/86 unaffected by this round (install.sh/README/ROADMAP only, no install-lib.sh changes).

known pre-existing gate failure (not ours, filed not fixed — 4a "anything else, different area"): `./run-tests.sh` Java boot-tests section fails on stock `main` (confirmed via `git stash`) — `BootTests.java:37-41` asserts stale evdev keycodes (`VK_A -> 30` etc.) that commit 370451a's keysym rework invalidated. Filed as https://github.com/ghostpsalm/DreamConnect/issues/19. When verifying this branch's gate, treat "Java boot tests" failure as expected/pre-existing and confirm the NEW "Installer shell tests" section + "Python daemon tests" section pass; do not let the pre-existing Java failure block this branch, and do not fix it here.

gate:
```
bash -n install.sh install-lib.sh test_install.sh
shellcheck install.sh install-lib.sh test_install.sh   # if available
./run-tests.sh   # Java BootTests + python3 -m unittest runtime/test_daemon.py + new Installer shell tests section
```
