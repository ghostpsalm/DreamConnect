# Hazards — DreamConnect (fragment: issue #38, total disjointness check)

Established at `df8f867` unless a row says otherwise. Breaker ran one lap: `no-defect-found`.

## Held — found once, now guarded by a test
| hazard | path | test that holds it | commit |
|---|---|---|---|
| `testAwtEvdevTablesDisjoint`'s bounded scan (vk `-0x100..0x20000`) could never see an overlap at an extended keycode `>= 0x01000000`, which `getExtendedKeyCodeForChar` can produce and `Robot.keyPress` accepts unvalidated. This was already flagged Unprobed in the #19 fragment. | `agent/test/dreamconnect/boot/BootTests.java` | `testAwtEvdevTablesDisjoint` (now total, via reflection over `AwtEvdev.KEY`/`KSYM`) — falsified by a scratch mutant at vk `0x01000000` that the old bounded version provably could not catch | df8f867 |

## Ruled out — probed with evidence, no defect found
| hypothesis | path | evidence | established at |
|---|---|---|---|
| Reflection (`Field.setAccessible(true)`) on `AwtEvdev.KEY`/`KSYM` needs JVM flags or fails under this repo's build. | `agent/test/dreamconnect/boot/BootTests.java` | `AwtEvdev` has no `module-info.java` (classpath app, not a JPMS module) — reflection works unrestricted; reproduced live by running the real gate. | df8f867 |
| A reflection bug (wrong field name, bad cast) could silently return an empty map and make the disjointness check pass vacuously. | `agent/test/dreamconnect/boot/BootTests.java` | Sanity floor (`KEY.size() >= 70`, `KSYM.size() >= 47`) demonstrated live by renaming a field in a scratch copy — throws `NoSuchFieldException` rather than passing quietly. | df8f867 |
| An unhandled exception from this test method (or any other declaring `throws Exception`) masks the rest of the gate run. | `agent/test/dreamconnect/boot/BootTests.java`, `run-tests.sh` | Real, reproduced (renaming `KEY` aborts the whole script under `set -e`, not just the Java section) — **not ruled out**, filed as its own issue instead since it's pre-existing and orthogonal to this change. See #41 and Unprobed below. |

## Unprobed — no oracle available
| hypothesis | path | what would settle it |
|---|---|---|
| Whether `run-tests.sh`'s test-invocation loop should catch `Throwable` per test method and continue, rather than aborting the whole gate on the first uncaught exception. Demonstrated live during this run's breaker pass (renaming `AwtEvdev.KEY` aborts the entire script, Python and shell suites included, not just the remaining Java tests) but is pre-existing harness behaviour, not introduced by this diff. | `agent/test/dreamconnect/boot/BootTests.java` (`main()`), `run-tests.sh` | A design decision plus a harness change; filed as #41. |
