# Hazards — DreamConnect (fragment: issue #36, fallbackKeysym's fate and the 86-vk contract)

Established at `800eeab` unless a row says otherwise. Breaker ran two laps (slice 1 alone, then
slices 2-5 cumulative); both `no-defect-found` beyond the doc-drift row already Held below.

## Held — found once, now guarded by a test
| hazard | path | test that holds it | commit |
|---|---|---|---|
| `runtime/README.md`'s `KS` row still described the printable-ASCII fallback ("plus the agent's printable-ASCII fallback") after `fallbackKeysym` was deleted — prose describing a route that no longer exists, with nothing to catch further drift. | `runtime/README.md:62` | No test guards prose; caught by the breaker's repo-wide grep for the deleted method's rationale, fixed same-slice. Re-check by grep on any future `fallbackKeysym`-adjacent change. | 800eeab |
| `docs/design.md`'s first draft of the "86-vk drop contract" section named only 5 of the 12 real members of the clipboard/edit/action cluster (missing `VK_ACCEPT/CANCEL/CLEAR/FIND/PROPS/STOP/COMPOSE`), and separately claimed no evdev keycode exists for a shifted symbol, which `KEY_DOLLAR`/`KEY_EURO` (input-event-codes.h) directly falsify for `VK_DOLLAR`/`VK_EURO_SIGN`. | `docs/design.md` | No automated test guards prose; a seraph re-verification independently reconstructed the full 69-vk list by reflection against `AwtEvdev.keycode`/`keysym` and confirmed exact membership/count match after the fix. Re-derive that reflection list before trusting the doc's cluster membership again if `AwtEvdev.java`'s tables change. | 800eeab |

## Ruled out — probed with evidence, no defect found
| hypothesis | path | evidence | established at |
|---|---|---|---|
| Some other caller reaches the deleted `fallbackKeysym` or assumed its behaviour. | `agent/boot/dreamconnect/boot/AwtEvdev.java`, `DreamConnectRobotPeer.java` | Repo-wide grep: zero remaining references outside historical `factory/` prose. `sendKey` was `fallbackKeysym`'s only caller; `keyPress`/`keyRelease` were `sendKey`'s only callers. | 800eeab |
| The daemon or its Python side assumes one wire line is always sent per `Robot.keyPress`/`keyRelease` call, so a silent drop desyncs it. | `runtime/dreamconnect_daemon.py` | `handle()` is fire-and-forget for every input command, returns `None` regardless of whether a line was ever sent — no ack, no sequence counter, no held-key state tracked anywhere in the repo (grepped `held|pressed|keyState`). | 800eeab |
| Adding the 17 new `KEY` table entries (F13-F24, HELP, KP-arrows) collides with an existing `KSYM` entry or otherwise breaks `testAwtEvdevTablesDisjoint`. | `agent/boot/dreamconnect/boot/AwtEvdev.java` | Disjointness re-run post-diff: 0 overlaps over the full bounded scan. None of the 17 new vk values pre-existed in either table (grep-confirmed). | 800eeab |
| `VK_KP_UP/DOWN/LEFT/RIGHT` sharing evdev codes with `VK_UP/DOWN/LEFT/RIGHT` creates an aliasing hazard — some code inverts evdev→vk, or tracks "is this key held" by evdev code, and a KP-arrow press paired with a plain-arrow release desyncs it. | `agent/boot/dreamconnect/boot/DreamConnectRobotPeer.java`, `runtime/dreamconnect_daemon.py` | No vk↔evdev reverse mapping or held-key state exists anywhere in the repo; both client classes forward stateless `K <code> <state>` lines. The daemon/Mutter-level question of a genuinely mismatched press/release pair is a live-guest question, not a code defect — see Unprobed below. | 800eeab |
| The new tests (`testAwtEvdevF13ToF24Mapped`, `testAwtEvdevHelpMapped`, `testAwtEvdevKpArrowsMapped`) are tautological, i.e. would pass under a wrong implementation (all-same-value bug, wrong table/route, transposed KP-arrow codes). | `agent/test/dreamconnect/boot/BootTests.java` | Each oracle validated discrimination against at least one plausible wrong implementation (all-F-keys-same-code, `k()` instead of `m()`, transposed KP-arrow pairing) in a scratch copy before handing off to the builder; all mutants produced the expected failures. | 800eeab |
| Repo-wide stale "86 unmapped" / "188 VK_ constants" counts left over after 17 of the 86 were mapped. | whole repo | Grepped for both numbers outside historical `factory/runs/*-notes.md` (point-in-time records, not living docs) — no stale count found in any current doc or test. | 800eeab |

## Unprobed — no oracle available
| hypothesis | path | what would settle it |
|---|---|---|
| Whether a real ScreenConnect session ever hands `Robot.keyPress` a raw int that is not any named `VK_*` constant — this bounds how much behaviour the `fallbackKeysym` deletion actually changes for a live user, versus being purely theoretical. | SC client → agent | A capture of SC's own key-event translation, or observed behaviour from a physical keyboard whose driver emits a raw AWT code outside the named enum. |
| Whether Mutter's `NotifyKeyboardKeycode` genuinely treats a KP-arrow press and a plain-arrow release (same evdev code, different originating AWT vk) as a clean press/release pair, or whether some other guest-side state (Num Lock toggling mid-press) could produce a stuck key. | Mutter / guest session, not this repo | A live ScreenConnect session against a Mutter guest, toggling Num Lock between a numpad-arrow press and its release. |
| Whether any of the 69 vks still dropped by contract (clipboard/edit/action keys, IME cluster, shifted-symbol vks, `VK_DEAD_*`) should instead map, per-key-class, on a guest where they're reachable (e.g. a Sun/Solaris-style keyboard for the action-key cluster, or a CJK IME session for the input-method cluster) — the contract in `docs/design.md` records the current decision, not a guest-side verification. | `agent/boot/dreamconnect/boot/AwtEvdev.java` | A product decision plus a guest-side test per key class, on hardware/software that actually exercises the cluster in question. |
| `VK_SEPARATOR`'s layout-independence gap (evdev 121 binds nothing on `us`/`pc`) — deliberately out of scope for #36. | `agent/boot/dreamconnect/boot/AwtEvdev.java` | Tracked as its own issue, #40. |
